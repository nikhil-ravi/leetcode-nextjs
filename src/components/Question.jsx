// import ReactHtmlParser from "react-html-parser";

export const Question = ({ question }) => {
  //   const { QID, Body } = question[0];
  console.log(question);
  return (
    <>
      {/* <span>Questions {QID}</span>
      <div>{ReactHtmlParser(Body)}</div> */}
    </>
  );
};
